Title: chore: add PR template and changelog

Summary:
- Adds a `.github/PULL_REQUEST_TEMPLATE.md` and `docs/CHANGELOG.md` to standardize PRs and release notes.

Changes:
- Creates PR template to guide contributors.
- Adds starter `CHANGELOG.md` under `docs/`.

Testing:
- No runtime changes. Small docs update only.

Notes:
- Use the PR template for the other open branches.
- Reviewers: @dragarys

Merge checklist:
- [ ] CI green
- [ ] Review approved
- [ ] Auto-merge enabled (optional)
